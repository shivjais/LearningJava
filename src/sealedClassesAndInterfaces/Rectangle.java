package sealedClassesAndInterfaces;

//because Shape is sealed then we have make this class either final, non-sealed or sealed
//here we make this class as non-sealed it means we are allowing any number of class to extend this class
public non-sealed class Rectangle implements Shape {
}
